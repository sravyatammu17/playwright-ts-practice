import { test, expect } from '@playwright/test';

test('Section 1: submit form with valid data shows confirmation', async ({ page }) => {
  await page.goto('https://www.sreenidhirajakrishnan.com/practice');

  // We'll fill in real locators once you tell me what's actually on the page —
  // open Section 1 and tell me the exact field labels/placeholders you see
});